import { moduleFor, test } from 'ember-qunit';

moduleFor('view:lista-examenes', 'Unit | View | lista examenes');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
